<% @ad.comments.each do |comment| %>
  <div class="row">
    <div class="col-md-12">
      <span><%= comment.member.profile_member.first_name %></span>
      <p><%= time_ago_in_words(comment.created_at) %></p>
      <p><%= comment.body %></p>
    </div>
  </div>
  <br>
<% end %>
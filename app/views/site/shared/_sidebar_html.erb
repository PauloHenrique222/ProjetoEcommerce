<div id="home-sidebar" class="col-lg-3">
  <h1 class="my-4 text-center">Shop Name</h1>
  <div class="list-group">
    <% @categories.each do |category| %>
      <%= link_to "#{category.description} (#{category.announcements.size})",
                  site_category_path(category.friendly_id), class: "list-group-item" %>
    <% end %>
  </div>
</div>